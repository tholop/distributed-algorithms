import sys

class U(process):

    def setup(initial_value, output_neighbours, D):
        self.initial_value = initial_value
        self.output_neighbours
        self.r_count = [0] * len(output_neighbours)
        self.s_count = 0
        self.D = 0
        self.X = [initial_value]
    
    def run():
        

    def receive(msg = M, from=sender):
        X.extend(M)
        r_count[sender] += 1
        



    